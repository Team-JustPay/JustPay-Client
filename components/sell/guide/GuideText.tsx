import styled from 'styled-components';

export const GuideText = styled.p`
  width: 100%;

  color: ${({ theme }) => theme.colors.gray3};
  font-size: 1.2rem;
  font-weight: 400;
  line-height: 1.7rem;

  text-align: center;
`;
